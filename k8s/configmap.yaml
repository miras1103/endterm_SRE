apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: microservices
data:
  DATABASE_URL: postgresql://app_user:app_password@postgres:5432/microservices_app
  JWT_SECRET: local-development-secret
